system-zero-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for system-zero.

Install with `sz install system-zero-ce` and run `sz doctor` from an initialized System Zero repo.
