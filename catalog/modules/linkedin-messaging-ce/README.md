linkedin-messaging-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for linkedin-messaging.

Install with `sz install linkedin-messaging-ce` and run `sz doctor` from an initialized System Zero repo.
