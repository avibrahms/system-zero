agent-dashboard-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for agent-dashboard.

Install with `sz install agent-dashboard-ce` and run `sz doctor` from an initialized System Zero repo.
