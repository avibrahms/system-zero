google-ads-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for google-ads.

Install with `sz install google-ads-ce` and run `sz doctor` from an initialized System Zero repo.
