start-preamble-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for start-preamble.

Install with `sz install start-preamble-ce` and run `sz doctor` from an initialized System Zero repo.
