chronicle-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for chronicle.

Install with `sz install chronicle-ce` and run `sz doctor` from an initialized System Zero repo.
