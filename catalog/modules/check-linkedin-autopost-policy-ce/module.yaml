id: check-linkedin-autopost-policy-ce
version: 0.1.0
category: connection-engine
description: Check Linkedin Autopost Policy reconstructed as a protocol-native self-improvement
  organ.
entry:
  type: python
  command: entry.py
  args: []
triggers:
- 'on': tick
provides:
- name: ce.check.linkedin.autopost.policy
  address: events:ce.check.linkedin.autopost.policy.snapshot
  description: Publishes Check Linkedin Autopost Policy reconstruction snapshots.
requires:
- providers:
  - bus
  - memory
  - discovery
setpoints: {}
hooks:
  reconcile: reconcile.sh
  doctor: doctor.sh
limits:
  max_runtime_seconds: 60
  max_memory_mb: 128
personas:
- static
- dynamic
