session-bootstrap-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for session-bootstrap.

Install with `sz install session-bootstrap-ce` and run `sz doctor` from an initialized System Zero repo.
