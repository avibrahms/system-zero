context-assembler-ce is an anonymized System Zero reconstruction module.

Mock absorbed module for context-assembler.

Install with `sz install context-assembler-ce` and run `sz doctor` from an initialized System Zero repo.
