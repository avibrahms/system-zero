sz_version: 0.1.0
host: generic
host_mode: install
modules:
  pulse-core:
    version: 0.1.0
    enabled: true
    setpoints:
      interval_seconds: 30
    bindings:
      immune.routing: immune-router
    quiet_hours:
      - "22:00-07:00"
providers:
  llm: mock
  vector: none
cloud:
  tier: free
